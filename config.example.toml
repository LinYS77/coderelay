# CodeRelay 0.3 is stateless: only CodeRelay's own API token hashes live on this server.
# Callers send TOTP/Outlook/FlySMS credentials in each authenticated POST request.
# All relative paths are resolved from this file's directory.

[server]
host = "127.0.0.1"
port = 8787
allowed_hosts = ["2fa.077.li", "localhost", "127.0.0.1"]
cors_origins = []
forwarded_allow_ips = "127.0.0.1"
access_log = false
log_level = "info"
max_wait_seconds = 30
http_connect_timeout_seconds = 5.0
http_read_timeout_seconds = 20.0
http_max_connections = 20
max_concurrent_code_requests = 10

[security]
api_token_hash_files = ["secrets/api-shared.sha256"]
strict_secret_permissions = true
api_rate_limit_per_minute = 60

[providers.outlook]
token_url = "https://login.microsoftonline.com/common/oauth2/v2.0/token"
imap_host = "outlook.office365.com"
imap_port = 993
imap_timeout_seconds = 15.0
poll_interval_seconds = 2.0
max_messages = 10
max_message_bytes = 262144

[providers.outlook.extractor]
sender_domains = []
subject_keywords = ["verification", "security code", "验证码"]
patterns = ['(?i)(?:code|验证码|安全码)\D{0,20}(?P<code>\d{6})']
max_age_seconds = 600
allow_generic_fallback = true
generic_requires_keyword = false
max_text_chars = 100000

[providers.flysms]
base_url = "https://flysms.xyz/icloud/api/pickup/messages"
poll_interval_seconds = 2.0
history_limit = 30
max_detail_messages = 5

[providers.flysms.extractor]
sender_domains = []
subject_keywords = ["verification", "security code", "验证码"]
patterns = ['(?i)(?:code|验证码|安全码)\D{0,20}(?P<code>\d{6})']
max_age_seconds = 600
allow_generic_fallback = true
generic_requires_keyword = false
max_text_chars = 100000
